Summarize the following normalized Obsidian canvas relationships JSON into one concise structured report.

Requirements:
- Return only one JSON object.
- Follow the provided output schema exactly.
- Keep `title` short and descriptive.
- Keep `overview` to one short paragraph.
- `group_count` must reflect the number of groups in the input.
- `ungrouped_node_count` must reflect the number of ungrouped nodes in the input.
- `key_points` should contain 3 to 5 concise bullet-like strings.
- Do not include markdown.
- Do not include keys outside the schema.

Input JSON:
{{{input_json}}}

Output schema:
{{{output_schema_json}}}
